{
  "name": "book-club-wrapped",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute book-club-db --local --file=./schema.sql && wrangler d1 execute book-club-db --local --file=./seed.sql",
    "db:init:remote": "wrangler d1 execute book-club-db --file=./schema.sql && wrangler d1 execute book-club-db --file=./seed.sql"
  },
  "dependencies": {
    "hono": "^4.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.5",
    "wrangler": "^4.61.1"
  }
}
